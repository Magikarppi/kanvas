import {
    CircularProgress
} from "@mui/material";

const Spinner = () => {
    return(
        <div>
            <CircularProgress color="secondary" />
        </div>
    );
};

export default Spinner;